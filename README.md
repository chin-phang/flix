# flix

```
# build docker image
docker build -t rhb/flix .

# run MySQL and movie app
docker-compose -f ./docker-compose.yml up -d

# go to API documentation
http://localhost:8080/swagger-ui.html
```
